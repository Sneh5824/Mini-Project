{
  "name": "blip-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace scripts for running Blip frontend and backend together",
  "scripts": {
    "install:all": "npm install --prefix backend; npm install --prefix frontend",
    "dev": "concurrently -n BACKEND,FRONTEND -c red,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
